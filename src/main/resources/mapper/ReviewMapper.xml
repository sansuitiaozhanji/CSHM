<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campus.mapper.ReviewMapper">

    <resultMap id="reviewMap" type="com.campus.entity.Review">
        <id property="id" column="id"/>
        <result property="orderId" column="order_id"/>
        <result property="productId" column="product_id"/>
        <result property="userId" column="user_id"/>
        <result property="rating" column="rating"/>
        <result property="content" column="content"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <resultMap id="reviewDetailMap" type="com.campus.entity.Review" extends="reviewMap">
        <association property="user" javaType="com.campus.entity.User">
            <id property="id" column="review_user_id"/>
            <result property="username" column="review_user_username"/>
            <result property="avatar" column="review_user_avatar"/>
        </association>
        <association property="product" javaType="com.campus.entity.Product">
            <id property="id" column="review_product_id"/>
            <result property="title" column="review_product_title"/>
        </association>
    </resultMap>

    <select id="findById" resultMap="reviewDetailMap">
        SELECT r.*,
               u.id as review_user_id, u.username as review_user_username, u.avatar as review_user_avatar,
               p.id as review_product_id, p.title as review_product_title
        FROM review r
        LEFT JOIN `user` u ON r.user_id = u.id
        LEFT JOIN product p ON r.product_id = p.id
        WHERE r.id = #{id}
    </select>

    <select id="findByProductId" resultMap="reviewDetailMap">
        SELECT r.*,
               u.id as review_user_id, u.username as review_user_username, u.avatar as review_user_avatar,
               p.id as review_product_id, p.title as review_product_title
        FROM review r
        LEFT JOIN `user` u ON r.user_id = u.id
        LEFT JOIN product p ON r.product_id = p.id
        WHERE r.product_id = #{productId} AND r.status = 1
        ORDER BY r.created_at DESC
    </select>

    <select id="findByUserId" resultMap="reviewDetailMap">
        SELECT r.*,
               u.id as review_user_id, u.username as review_user_username, u.avatar as review_user_avatar,
               p.id as review_product_id, p.title as review_product_title
        FROM review r
        LEFT JOIN `user` u ON r.user_id = u.id
        LEFT JOIN product p ON r.product_id = p.id
        WHERE r.user_id = #{userId}
        ORDER BY r.created_at DESC
    </select>

    <select id="findByOrderId" resultMap="reviewMap">
        SELECT * FROM review WHERE order_id = #{orderId}
    </select>

    <select id="findAll" resultMap="reviewDetailMap">
        SELECT r.*,
               u.id as review_user_id, u.username as review_user_username, u.avatar as review_user_avatar,
               p.id as review_product_id, p.title as review_product_title
        FROM review r
        LEFT JOIN `user` u ON r.user_id = u.id
        LEFT JOIN product p ON r.product_id = p.id
        ORDER BY r.created_at DESC
    </select>

    <insert id="insert" parameterType="com.campus.entity.Review" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO review (order_id, product_id, user_id, rating, content, status, created_at)
        VALUES (#{orderId}, #{productId}, #{userId}, #{rating}, #{content}, 1, NOW())
    </insert>

    <update id="updateStatus">
        UPDATE review SET status = #{status} WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM review WHERE id = #{id}
    </delete>

</mapper>
