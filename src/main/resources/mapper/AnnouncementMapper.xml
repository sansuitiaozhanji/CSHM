<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campus.mapper.AnnouncementMapper">

    <resultMap id="announcementMap" type="com.campus.entity.Announcement">
        <id property="id" column="id"/>
        <result property="title" column="title"/>
        <result property="content" column="content"/>
        <result property="type" column="type"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findById" resultMap="announcementMap">
        SELECT * FROM announcement WHERE id = #{id}
    </select>

    <select id="findAll" resultMap="announcementMap">
        SELECT * FROM announcement ORDER BY created_at DESC
    </select>

    <select id="findPublished" resultMap="announcementMap">
        SELECT * FROM announcement WHERE status = 1
        <if test="type != null">
            AND type = #{type}
        </if>
        ORDER BY created_at DESC
    </select>

    <insert id="insert" parameterType="com.campus.entity.Announcement" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO announcement (title, content, type, status, created_at, updated_at)
        VALUES (#{title}, #{content}, #{type}, #{status}, NOW(), NOW())
    </insert>

    <update id="update" parameterType="com.campus.entity.Announcement">
        UPDATE announcement SET title = #{title}, content = #{content}, type = #{type}, status = #{status}, updated_at = NOW()
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM announcement WHERE id = #{id}
    </delete>

</mapper>
